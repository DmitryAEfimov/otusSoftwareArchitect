apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "device-library.fullname" . }}-test-connection"
  labels:
    {{- include "device-library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "device-library.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
