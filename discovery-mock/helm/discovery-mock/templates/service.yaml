apiVersion: v1
kind: Service
metadata:
  name: {{ include "discovery-mock.fullname" . }}
  labels:
    {{- include "discovery-mock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "discovery-mock.selectorLabels" . | nindent 4 }}
